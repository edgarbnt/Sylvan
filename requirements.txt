# Sylvan — Python dependencies (CPU). Install into a venv:
#   python -m venv env && source env/bin/activate && pip install -r requirements.txt
# The physics/environment runs in Godot 4 (separate binary under tools/godot/), not pip.
torch>=2.5          # CPU build; the world-model, PPO residual, planner, value heads
numpy>=1.26         # tensors / data plumbing everywhere
matplotlib>=3.8     # diagnostics + the foraging visualization (tools/make_forage_gif.py)
pillow>=10.0        # GIF writer for the visualization
