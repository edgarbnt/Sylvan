# Archi-HUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Une page web locale qui affiche en permanence les modules de l'architecture Sylvan (état + rôle JEPA + ancre code) et les anime quand un run foraging tourne.

**Architecture:** Data-driven. Un moteur de rendu vanilla (HTML/JS/CSS) bête et stable lit `architecture.json` (source de vérité conceptuelle, maintenue à la main) et poll `data/hud/live.json` (valeurs runtime écrites par le serveur planner). Aucune info d'archi dans le code de rendu.

**Tech Stack:** HTML/CSS/JavaScript vanilla (zéro build, zéro framework) ; Python 3.12 (venv `env_pytorch_3.12`) pour le validateur et l'écriture de `live.json` ; `python -m http.server` pour servir.

## Global Constraints

- venv : `env_pytorch_3.12/bin/python` (CPU). Lancer depuis la racine avec `PYTHONPATH=python`.
- **Pas d'horloge murale dans les scripts** (règle projet) : `live.json.ts` = entier monotone, jamais un timestamp réel.
- Le HUD est un méta-outil : il LIT/DÉCRIT le pipeline, il ne le modifie jamais. L'intégration dans `serve_planner_command` doit être **inerte par défaut** (gate `SYLVAN_HUD=1`), pour ne pas risquer le chemin vivant.
- `fetch()` sur `file://` est bloqué par le navigateur → toujours servir via HTTP local.
- Suivre l'idiome du repo : scripts autonomes lancés via le venv (pas de framework de test JS introduit).
- États autorisés d'un module : `pur`, `partiel`, `echafaudage`, `manquant`.

---

### Task 1: Donnée conceptuelle + validateur

**Files:**
- Create: `tools/archi_hud/architecture.json`
- Create: `tools/archi_hud/validate_architecture.py`

**Interfaces:**
- Produces: `tools/archi_hud/architecture.json` avec la forme `{ "focus": str, "modules": [ {id, etat, titre, couche, quoi, detail, code, live_field, depends_on} ] }`. `code` = chemin repo-relatif `path[:lines]` ou `null`. `etat` ∈ {pur, partiel, echafaudage, manquant}. Tous les `depends_on` et `focus` référencent des `id` existants. Le moteur de rendu (Task 2) et l'écriture live (Task 4) en dépendent.

- [ ] **Step 1: Écrire le validateur (le test échoue car le JSON n'existe pas encore)**

Create `tools/archi_hud/validate_architecture.py`:

```python
#!/usr/bin/env python3
"""Valide tools/archi_hud/architecture.json : schéma, états, références d'id, ancres code.
Idiome repo : script autonome, sort != 0 si invalide. Lancer : env_pytorch_3.12/bin/python tools/archi_hud/validate_architecture.py"""
import json
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
ARCHI = os.path.join(ROOT, "tools", "archi_hud", "architecture.json")
ALLOWED_ETATS = {"pur", "partiel", "echafaudage", "manquant"}
REQUIRED_KEYS = {"id", "etat", "titre", "couche", "quoi", "detail", "code", "live_field", "depends_on"}


def validate(path: str) -> list[str]:
    errs: list[str] = []
    with open(path, encoding="utf-8") as f:
        data = json.load(f)
    modules = data.get("modules")
    if not isinstance(modules, list) or not modules:
        return ["'modules' doit être une liste non vide"]
    ids = {m.get("id") for m in modules}
    if data.get("focus") not in ids:
        errs.append(f"focus '{data.get('focus')}' n'est pas un id de module")
    seen = set()
    for m in modules:
        mid = m.get("id", "?")
        missing = REQUIRED_KEYS - set(m)
        if missing:
            errs.append(f"[{mid}] clés manquantes : {sorted(missing)}")
            continue
        if mid in seen:
            errs.append(f"[{mid}] id dupliqué")
        seen.add(mid)
        if m["etat"] not in ALLOWED_ETATS:
            errs.append(f"[{mid}] etat invalide '{m['etat']}' (autorisés : {sorted(ALLOWED_ETATS)})")
        if not isinstance(m["depends_on"], list):
            errs.append(f"[{mid}] depends_on doit être une liste")
        else:
            for dep in m["depends_on"]:
                if dep not in ids:
                    errs.append(f"[{mid}] depends_on référence un id inconnu : '{dep}'")
        if m["live_field"] is not None and not isinstance(m["live_field"], str):
            errs.append(f"[{mid}] live_field doit être str ou null")
        code = m["code"]
        if code is not None:
            relpath = code.split(":", 1)[0]
            if not os.path.exists(os.path.join(ROOT, relpath)):
                errs.append(f"[{mid}] ancre code introuvable : '{relpath}'")
        elif m["etat"] != "manquant":
            errs.append(f"[{mid}] code null n'est autorisé que pour etat 'manquant'")
    return errs


def main() -> int:
    if not os.path.exists(ARCHI):
        print(f"FAIL: {ARCHI} introuvable")
        return 1
    errs = validate(ARCHI)
    if errs:
        print("FAIL:")
        for e in errs:
            print(f"  - {e}")
        return 1
    print("OK: architecture.json valide")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Lancer le validateur, vérifier qu'il échoue (JSON absent)**

Run: `cd /home/edgarbrunet/Documents/PERSO/SylvanV1 && env_pytorch_3.12/bin/python tools/archi_hud/validate_architecture.py`
Expected: `FAIL: .../architecture.json introuvable` et code de sortie 1.

- [ ] **Step 3: Créer la donnée `tools/archi_hud/architecture.json`**

```json
{
  "focus": "transport_slot",
  "modules": [
    {
      "id": "perception_slot",
      "etat": "pur",
      "titre": "Perception / Slot",
      "couche": "perception",
      "quoi": "Localise la nourriture depuis les rayons couleur bruts (vision), 100% label-free",
      "detail": "Slot auto-supervisé (slot_head) : attention géométrique soft-argmax sur 36 rayons, gatée par la saillance perceptuelle. Entraîné SANS label (consistance de transport Rot(+Δyaw) + VICReg). A éliminé l'oracle de position de la boucle (promu live 2026-06-24).",
      "code": "python/sylvan/models/slot_head.py",
      "live_field": "bearing",
      "depends_on": []
    },
    {
      "id": "world_model",
      "etat": "partiel",
      "titre": "World Model",
      "couche": "wm",
      "quoi": "Imagine la dynamique en espace latent (rêve open-loop des déplacements)",
      "detail": "WM purifié (recon droppée 2026-06-25 → JEPA principe n°1 : prédire la représentation, pas reconstruire l'entrée). RESTE impur : déterministe + sans incertitude (bloque la curiosité). C'est la grosse refonte object-centric différée.",
      "code": "data/checkpoints/wm_rich_fidele_sym_jepa/wm_best.pt",
      "live_field": null,
      "depends_on": ["perception_slot"]
    },
    {
      "id": "transport_slot",
      "etat": "echafaudage",
      "titre": "Transport du slot",
      "couche": "planner",
      "quoi": "Transporte la position de l'objet à la main (trigo) au fil du rêve",
      "detail": "BÉQUILLE : l'objet est une coordonnée FIGÉE à t=0 (perception apprise ✅) puis transportée par une boucle trigo (sin/cos) DANS le planner. Le WM n'est jamais interrogé sur 'où est l'objet maintenant' — l'object-permanence vit hors du WM. À internaliser dans le latent du WM (chantier courant).",
      "code": "python/sylvan/control/planning/command_planner.py:204-218",
      "live_field": "min_dist",
      "depends_on": ["world_model", "perception_slot"]
    },
    {
      "id": "cout_planner",
      "etat": "pur",
      "titre": "Coût planner",
      "couche": "planner",
      "quoi": "Score d'un plan = -distance à la nourriture (coût intrinsèque pur)",
      "detail": "Coût = -min_dist PUR (heading_weight retiré 2026-06-25 : le slot précis à 4.9° rend le how-to-hint inutile). La nourriture n'existe QUE dans ce coût (corps/WM/CPG food-agnostiques, BLUEPRINT §14).",
      "code": "python/sylvan/control/planning/command_planner.py:226",
      "live_field": null,
      "depends_on": ["transport_slot", "drives"]
    },
    {
      "id": "planner_mpc",
      "etat": "pur",
      "titre": "Planner MPC",
      "couche": "planner",
      "quoi": "Cherche la meilleure commande (vx, ω) en espace-commande sur l'horizon",
      "detail": "Actor haut-niveau : déroule des séquences de commandes dans le WM, score chacune par le coût, renvoie la meilleure (vx, ω). Le plan moteur ('oriente puis avance') ÉMERGE du coût.",
      "code": "python/sylvan/control/planning/command_planner.py:166",
      "live_field": "command",
      "depends_on": ["cout_planner", "world_model"]
    },
    {
      "id": "residu_ppo",
      "etat": "pur",
      "titre": "Résidu PPO",
      "couche": "moteur",
      "quoi": "Petit résidu borné qui rend la marche rapide ET stable",
      "detail": "Actor bas-niveau : corrige le CPG codé-main (équilibre/perf), gain borné 0.4. Warm-start hexapod_v2 + symétrie. Banké comme base motrice finale.",
      "code": "data/checkpoints/hexapod_v2/policy_best.pt",
      "live_field": null,
      "depends_on": ["planner_mpc"]
    },
    {
      "id": "corps_cpg",
      "etat": "pur",
      "titre": "Corps / CPG",
      "couche": "moteur",
      "quoi": "Hexapode + générateur de marche (CPG) : marche et tourne par construction",
      "detail": "Substrat moteur. Le CPG produit la démarche trépied et le virage par construction ; le corps hexapode est stable (6 pattes) → l'énergie va à la propulsion. 0.49 m/s, virages symétriques, fall 0%.",
      "code": "data/checkpoints/hexapod_v2/policy_best.pt",
      "live_field": "fwd_v",
      "depends_on": ["residu_ppo"]
    },
    {
      "id": "drives",
      "etat": "pur",
      "titre": "Drives faim/soif",
      "couche": "pulsions",
      "quoi": "Pulsions homéostatiques (faim, soif) qui pondèrent le coût",
      "detail": "Propriété du CORPS (définie une fois). L'arbitrage affamé→bouffe / assoiffé→eau ÉMERGE (non codé). Eau/soif = inputs planner-only, WM inchangé (étage 1).",
      "code": "python/sylvan/control/planning/command_planner.py:249",
      "live_field": "energy",
      "depends_on": []
    },
    {
      "id": "memoire_spatiale",
      "etat": "manquant",
      "titre": "Mémoire spatiale",
      "couche": "manquant",
      "quoi": "Se souvenir où est un objet quand on ne le voit plus (permanence)",
      "detail": "MANQUANT (Short-term memory de LeCun). Tombe gratuitement une fois le slot internalisé dans le WM : un slot non-perçu persiste par dead-reckoning (permanence prouvée F-pure-3 à +0.95).",
      "code": null,
      "live_field": null,
      "depends_on": ["perception_slot", "world_model"]
    },
    {
      "id": "critique_appris",
      "etat": "manquant",
      "titre": "Critique appris",
      "couche": "manquant",
      "quoi": "Une valeur apprise V(slot) = gain futur, à la place du coût géométrique",
      "detail": "MANQUANT (Critic de LeCun). Remplacerait -min_dist par un critique appris sur le slot ; gagnera sa place en multi-drive foresighted où la géométrie ne suffit plus.",
      "code": null,
      "live_field": null,
      "depends_on": ["world_model"]
    },
    {
      "id": "configurator",
      "etat": "manquant",
      "titre": "Configurator",
      "couche": "manquant",
      "quoi": "Chef d'orchestre : module l'attention et arbitre les pulsions selon le but",
      "detail": "MANQUANT (Configurator de LeCun). Au-dessus de tout : configure perception/WM/coût selon la tâche du moment. Chantier lointain.",
      "code": null,
      "live_field": null,
      "depends_on": ["drives"]
    }
  ]
}
```

- [ ] **Step 4: Lancer le validateur, vérifier qu'il passe**

Run: `cd /home/edgarbrunet/Documents/PERSO/SylvanV1 && env_pytorch_3.12/bin/python tools/archi_hud/validate_architecture.py`
Expected: `OK: architecture.json valide` et code de sortie 0.

- [ ] **Step 5: Commit**

```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
git add tools/archi_hud/architecture.json tools/archi_hud/validate_architecture.py
git commit -m "Archi-HUD: donnée conceptuelle architecture.json + validateur"
```

---

### Task 2: Moteur de rendu (carte conceptuelle)

**Files:**
- Create: `tools/archi_hud/index.html`
- Create: `tools/archi_hud/style.css`
- Create: `tools/archi_hud/app.js`

**Interfaces:**
- Consumes: `architecture.json` (Task 1).
- Produces: une page qui rend les modules en colonne (ordre du tableau = ordre du flux), couleur par `etat`, badge `◀ ON BOSSE ICI` sur `focus`, panneau latéral au clic (déplie `detail` + `code`), chips `alimenté par` (depends_on). `app.js` expose la fonction globale `renderArchitecture(data)` et `startLivePolling()` (cette dernière complétée en Task 5 ; en Task 2 c'est un stub vide).

- [ ] **Step 1: Créer `tools/archi_hud/index.html`**

```html
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sylvan — carte de l'architecture</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>Sylvan — architecture</h1>
    <div id="mode" class="mode conceptuel">mode conceptuel</div>
    <ul id="legend" class="legend">
      <li class="etat-pur">pur</li>
      <li class="etat-partiel">partiel</li>
      <li class="etat-echafaudage">échafaudage</li>
      <li class="etat-manquant">manquant</li>
    </ul>
  </header>
  <main>
    <section id="pipeline" class="pipeline" aria-label="pipeline des modules"></section>
    <aside id="panel" class="panel hidden">
      <button id="panel-close" class="panel-close" aria-label="fermer">×</button>
      <div id="panel-body"></div>
    </aside>
  </main>
  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: Créer `tools/archi_hud/style.css`**

```css
:root {
  --pur: #2ecc71; --partiel: #f1c40f; --echafaudage: #e67e22; --manquant: #e74c3c;
  --bg: #11161c; --card: #1b232c; --text: #e6edf3; --muted: #8b98a5;
}
* { box-sizing: border-box; }
body { margin: 0; font-family: system-ui, sans-serif; background: var(--bg); color: var(--text); }
header { display: flex; align-items: center; gap: 1.5rem; padding: 0.75rem 1.25rem; border-bottom: 1px solid #2a3540; flex-wrap: wrap; }
header h1 { font-size: 1.1rem; margin: 0; }
.mode { font-size: 0.8rem; padding: 0.2rem 0.6rem; border-radius: 1rem; }
.mode.conceptuel { background: #2a3540; color: var(--muted); }
.mode.live { background: #1e3a2a; color: var(--pur); animation: blink 1s infinite; }
@keyframes blink { 50% { opacity: 0.55; } }
.legend { list-style: none; display: flex; gap: 0.9rem; margin: 0; padding: 0; font-size: 0.78rem; }
.legend li::before { content: ""; display: inline-block; width: 0.7rem; height: 0.7rem; border-radius: 2px; margin-right: 0.3rem; vertical-align: middle; }
.legend .etat-pur::before { background: var(--pur); }
.legend .etat-partiel::before { background: var(--partiel); }
.legend .etat-echafaudage::before { background: var(--echafaudage); }
.legend .etat-manquant::before { background: var(--manquant); }
main { display: flex; gap: 1rem; padding: 1.25rem; align-items: flex-start; }
.pipeline { display: flex; flex-direction: column; gap: 0.4rem; max-width: 560px; flex: 1; }
.node { background: var(--card); border-left: 5px solid var(--muted); border-radius: 6px; padding: 0.6rem 0.85rem; cursor: pointer; position: relative; transition: transform 0.1s; }
.node:hover { transform: translateX(3px); }
.node.etat-pur { border-left-color: var(--pur); }
.node.etat-partiel { border-left-color: var(--partiel); }
.node.etat-echafaudage { border-left-color: var(--echafaudage); }
.node.etat-manquant { border-left-color: var(--manquant); opacity: 0.72; border-left-style: dashed; }
.node .titre { font-weight: 600; }
.node .quoi { font-size: 0.82rem; color: var(--muted); margin-top: 0.15rem; }
.node .deps { font-size: 0.7rem; color: var(--muted); margin-top: 0.35rem; }
.node .deps span { background: #2a3540; border-radius: 3px; padding: 0.05rem 0.35rem; margin-right: 0.25rem; }
.node .focus-badge { position: absolute; top: 0.5rem; right: 0.6rem; font-size: 0.68rem; color: #ffd479; }
.node.focus { outline: 2px solid #ffd479; box-shadow: 0 0 12px rgba(255,212,121,0.35); }
.node .live-val { position: absolute; bottom: 0.5rem; right: 0.6rem; font-size: 0.75rem; font-family: monospace; color: var(--pur); }
.node.pulse { animation: pulse 0.6s; }
@keyframes pulse { 0% { background: #243447; } 100% { background: var(--card); } }
.spine { width: 0; height: 0.4rem; border-left: 2px dashed #2a3540; margin-left: 1.1rem; }
.panel { width: 320px; background: var(--card); border: 1px solid #2a3540; border-radius: 6px; padding: 1rem; position: sticky; top: 1.25rem; }
.panel.hidden { display: none; }
.panel-close { float: right; background: none; border: none; color: var(--muted); font-size: 1.3rem; cursor: pointer; }
.panel h2 { margin: 0 0 0.25rem; font-size: 1rem; }
.panel .pill { display: inline-block; font-size: 0.72rem; padding: 0.1rem 0.5rem; border-radius: 1rem; margin-bottom: 0.6rem; }
.panel .detail { font-size: 0.85rem; line-height: 1.45; }
.panel .code { font-family: monospace; font-size: 0.78rem; color: #7fb0ff; margin-top: 0.8rem; word-break: break-all; }
```

- [ ] **Step 3: Créer `tools/archi_hud/app.js` (rendu + stub live)**

```javascript
"use strict";

const ETAT_LABEL = { pur: "pur", partiel: "partiel", echafaudage: "échafaudage", manquant: "manquant" };
let MODULES_BY_ID = {};

function nodeEl(m, focus) {
  const el = document.createElement("div");
  el.className = `node etat-${m.etat}` + (m.id === focus ? " focus" : "");
  el.dataset.id = m.id;
  if (m.live_field) el.dataset.liveField = m.live_field;
  const deps = (m.depends_on || [])
    .map((d) => `<span>${(MODULES_BY_ID[d] || {}).titre || d}</span>`)
    .join("");
  el.innerHTML =
    `<div class="titre">${m.titre}</div>` +
    `<div class="quoi">${m.quoi}</div>` +
    (deps ? `<div class="deps">⬆ alimenté par : ${deps}</div>` : "") +
    (m.id === focus ? `<div class="focus-badge">◀ ON BOSSE ICI</div>` : "") +
    `<div class="live-val"></div>`;
  el.addEventListener("click", () => openPanel(m));
  return el;
}

function openPanel(m) {
  const body = document.getElementById("panel-body");
  body.innerHTML =
    `<h2>${m.titre}</h2>` +
    `<span class="pill etat-${m.etat}" style="background:#2a3540">${ETAT_LABEL[m.etat]}</span>` +
    `<div class="detail">${m.detail}</div>` +
    (m.code ? `<div class="code">📄 ${m.code}</div>` : `<div class="code">— pas encore de code —</div>`);
  document.getElementById("panel").classList.remove("hidden");
}

function renderArchitecture(data) {
  MODULES_BY_ID = {};
  data.modules.forEach((m) => (MODULES_BY_ID[m.id] = m));
  const pipeline = document.getElementById("pipeline");
  pipeline.innerHTML = "";
  data.modules.forEach((m, i) => {
    pipeline.appendChild(nodeEl(m, data.focus));
    if (i < data.modules.length - 1) {
      const spine = document.createElement("div");
      spine.className = "spine";
      pipeline.appendChild(spine);
    }
  });
}

function startLivePolling() {
  /* complété en Task 5 */
}

async function init() {
  document.getElementById("panel-close").addEventListener("click", () =>
    document.getElementById("panel").classList.add("hidden")
  );
  const data = await fetch("architecture.json").then((r) => r.json());
  renderArchitecture(data);
  startLivePolling();
}

init();
```

- [ ] **Step 4: Servir et vérifier visuellement**

Run:
```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1/tools/archi_hud
../../env_pytorch_3.12/bin/python -m http.server 8765 &
SRV=$!
sleep 1
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8765/index.html
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8765/architecture.json
kill $SRV
```
Expected: deux fois `200`.

Puis ouvrir `http://127.0.0.1:8765/index.html` dans un navigateur (relancer le serveur sans `kill`) et vérifier la checklist :
- 11 cartes en colonne, couleur de bordure conforme à l'état (perception/coût/planner/résidu/corps/drives = vert, world_model = jaune, transport = orange, les 3 manquants = rouge pointillé estompé).
- La carte « Transport du slot » porte le halo + badge `◀ ON BOSSE ICI`.
- Clic sur une carte → panneau latéral avec le `detail` complet + la ligne `📄 code`.
- Bandeau « mode conceptuel » visible (pas de valeurs live).

- [ ] **Step 5: Commit**

```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
git add tools/archi_hud/index.html tools/archi_hud/style.css tools/archi_hud/app.js
git commit -m "Archi-HUD: moteur de rendu vanilla (carte conceptuelle)"
```

---

### Task 3: Lanceur `voir_archi.sh`

**Files:**
- Create: `voir_archi.sh`

**Interfaces:**
- Consumes: le dossier `tools/archi_hud/` (Tasks 1-2).
- Produces: `bash voir_archi.sh` sert `tools/archi_hud/` sur un port local et ouvre le navigateur sur `index.html`. Valide d'abord `architecture.json` (refuse de servir une carte qui ment).

- [ ] **Step 1: Créer `voir_archi.sh`**

```bash
#!/bin/zsh
# Carte vivante de l'architecture Sylvan. Valide la donnée puis sert la page + ouvre le navigateur.
# Usage: bash voir_archi.sh [port=8765]
set -e
ROOT=/home/edgarbrunet/Documents/PERSO/SylvanV1; cd "$ROOT"
PORT=${1:-8765}
# Refuse de servir une carte invalide (anti-mensonge).
./env_pytorch_3.12/bin/python tools/archi_hud/validate_architecture.py
URL="http://127.0.0.1:${PORT}/index.html"
echo "Archi-HUD → $URL  (Ctrl-C pour arrêter)"
( sleep 1; (xdg-open "$URL" >/dev/null 2>&1 || echo "Ouvre $URL dans ton navigateur") ) &
cd tools/archi_hud
exec "$ROOT/env_pytorch_3.12/bin/python" -m http.server "$PORT" --bind 127.0.0.1
```

- [ ] **Step 2: Vérifier que le lanceur valide puis sert**

Run:
```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
timeout 4 bash voir_archi.sh 8766 > /tmp/voir_archi.log 2>&1 &
sleep 2.5
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8766/index.html
grep -q "architecture.json valide" /tmp/voir_archi.log && echo "VALIDATION_OK"
wait 2>/dev/null
```
Expected: `200` puis `VALIDATION_OK`.

- [ ] **Step 3: Commit**

```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
git add voir_archi.sh
git commit -m "Archi-HUD: lanceur voir_archi.sh (valide + sert + ouvre)"
```

---

### Task 4: Écriture de `live.json` (producteur)

**Files:**
- Create: `python/sylvan/hud/__init__.py`
- Create: `python/sylvan/hud/live_writer.py`
- Create: `python/sylvan/hud/test_live_writer.py`
- Create: `data/hud/.gitignore`
- Modify: `python/scripts/serve_planner_command.py`

**Interfaces:**
- Produces: `write_live(path: str, *, ts: int, episode: int, step: int, fields: dict) -> None` — écrit atomiquement `{"ts", "episode", "step", "fields"}` en JSON. Consommé par le polling de Task 5.
- Consumes: dans `serve_planner_command`, les valeurs déjà calculées dans `predict_full` (`food_pos`, `self._cmd`, `energy`, `thirst`, et `pred_min_dist` du plan).

- [ ] **Step 1: Écrire le test du writer (échoue : module absent)**

Create `python/sylvan/hud/test_live_writer.py`:

```python
"""Test autonome du writer live.json. Lancer : PYTHONPATH=python env_pytorch_3.12/bin/python python/sylvan/hud/test_live_writer.py"""
import json
import os
import tempfile

from sylvan.hud.live_writer import write_live


def test_writes_payload_and_is_atomic():
    d = tempfile.mkdtemp()
    path = os.path.join(d, "sub", "live.json")  # sous-dossier inexistant → doit être créé
    write_live(path, ts=3, episode=2, step=412, fields={"min_dist": 1.8, "command": [0.7, -0.3]})
    with open(path, encoding="utf-8") as f:
        data = json.load(f)
    assert data["ts"] == 3, data
    assert data["episode"] == 2 and data["step"] == 412, data
    assert data["fields"]["min_dist"] == 1.8, data
    assert data["fields"]["command"] == [0.7, -0.3], data
    # pas de fichier temporaire laissé derrière
    assert os.listdir(os.path.dirname(path)) == ["live.json"], os.listdir(os.path.dirname(path))


if __name__ == "__main__":
    test_writes_payload_and_is_atomic()
    print("OK: live_writer")
```

- [ ] **Step 2: Lancer le test, vérifier l'échec (ModuleNotFoundError)**

Run: `cd /home/edgarbrunet/Documents/PERSO/SylvanV1 && PYTHONPATH=python env_pytorch_3.12/bin/python python/sylvan/hud/test_live_writer.py`
Expected: `ModuleNotFoundError: No module named 'sylvan.hud'`.

- [ ] **Step 3: Créer le module `python/sylvan/hud/__init__.py` (vide) et `live_writer.py`**

`python/sylvan/hud/__init__.py` : fichier vide.

`python/sylvan/hud/live_writer.py` :
```python
"""Écriture atomique de data/hud/live.json (valeurs runtime pour l'Archi-HUD).
Pas d'horloge murale (règle projet) : `ts` est un entier monotone fourni par l'appelant."""
import json
import os
import tempfile


def write_live(path: str, *, ts: int, episode: int, step: int, fields: dict) -> None:
    """Écrit {ts, episode, step, fields} en JSON, de façon atomique (tmp + os.replace).
    Crée le dossier parent au besoin. Ne lève jamais sur un champ non-sérialisable :
    les valeurs non-JSON sont ignorées (le HUD est best-effort, jamais bloquant)."""
    directory = os.path.dirname(path) or "."
    os.makedirs(directory, exist_ok=True)
    safe_fields = {}
    for k, v in fields.items():
        try:
            json.dumps(v)
            safe_fields[k] = v
        except (TypeError, ValueError):
            continue
    payload = {"ts": int(ts), "episode": int(episode), "step": int(step), "fields": safe_fields}
    fd, tmp = tempfile.mkstemp(dir=directory, suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            json.dump(payload, f)
        os.replace(tmp, path)
    except Exception:
        if os.path.exists(tmp):
            os.remove(tmp)
        raise
```

- [ ] **Step 4: Lancer le test, vérifier qu'il passe**

Run: `cd /home/edgarbrunet/Documents/PERSO/SylvanV1 && PYTHONPATH=python env_pytorch_3.12/bin/python python/sylvan/hud/test_live_writer.py`
Expected: `OK: live_writer`.

- [ ] **Step 5: Créer `data/hud/.gitignore` (ignore les valeurs runtime)**

`data/hud/.gitignore` :
```
live.json
```

- [ ] **Step 6: Câbler l'écriture dans `serve_planner_command.py` (inerte sauf `SYLVAN_HUD=1`)**

Dans `python/scripts/serve_planner_command.py`, ajouter l'import en tête (près de `import json`, ~ligne 20) :
```python
import math
import os
from sylvan.hud.live_writer import write_live
```
(si `import math`/`import os` existent déjà, ne pas les dupliquer.)

Dans `_PlannerService.__init__` (après les autres initialisations d'attributs, ~ligne 102 après `self._food_pos_ema = None`), ajouter :
```python
        self.hud_enable = os.environ.get("SYLVAN_HUD") == "1"
        self.hud_ts = 0
        self.hud_path = os.environ.get("SYLVAN_HUD_PATH", "data/hud/live.json")
        self._last_min_dist = float("nan")
```

Dans `predict_full`, capturer `pred_min_dist` du plan slot. Remplacer le bloc (~lignes 196-203) :
```python
                elif self.localizer is not None:
                    # LOCALISATION = perception APPRISE (slot pur si chargé, sinon retina_head). water override seulement
                    # si la tête gère l'eau ; sinon on garde l'EMA radar eau (2ᵉ pulsion non encore rétinisée).
                    self._cmd = self.planner.plan(
                        wm_obs, self._radar_ema,
                        water_radar=None if self._retina_n_res > 1 else self._water_ema,
                        energy=energy / 100.0, thirst=thirst / 100.0,
                        override_pos=True, food_override=food_pos,
                        water_override=water_pos if self._retina_n_res > 1 else (
                            None),
                    )["command"]
```
par :
```python
                elif self.localizer is not None:
                    # LOCALISATION = perception APPRISE (slot pur si chargé, sinon retina_head). water override seulement
                    # si la tête gère l'eau ; sinon on garde l'EMA radar eau (2ᵉ pulsion non encore rétinisée).
                    plan_res = self.planner.plan(
                        wm_obs, self._radar_ema,
                        water_radar=None if self._retina_n_res > 1 else self._water_ema,
                        energy=energy / 100.0, thirst=thirst / 100.0,
                        override_pos=True, food_override=food_pos,
                        water_override=water_pos if self._retina_n_res > 1 else (
                            None),
                    )
                    self._cmd = plan_res["command"]
                    self._last_min_dist = float(plan_res.get("pred_min_dist", float("nan")))
```

Juste après `self._ticks += 1` et `vx, om = self._cmd` (~ligne 212), avant le calcul du résidu, ajouter l'écriture HUD (best-effort, jamais bloquante) :
```python
            if self.hud_enable:
                self.hud_ts += 1
                fields = {"command": [float(vx), float(om)], "energy": float(energy), "thirst": float(thirst)}
                if food_pos is not None:
                    fields["bearing"] = math.degrees(math.atan2(float(food_pos[0]), float(food_pos[1])))
                if self._last_min_dist == self._last_min_dist:  # pas NaN
                    fields["min_dist"] = self._last_min_dist
                try:
                    write_live(self.hud_path, ts=self.hud_ts, episode=0, step=self._ticks, fields=fields)
                except Exception:
                    pass
```

- [ ] **Step 7: Vérifier que le serveur démarre toujours, HUD inerte par défaut**

Run:
```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
PYTHONPATH=python timeout 8 env_pytorch_3.12/bin/python -m scripts.serve_planner_command \
  --wm data/checkpoints/wm_rich_fidele_sym_jepa/wm_best.pt \
  --residual data/checkpoints/hexapod_v2/policy_best.pt \
  --slot-head data/checkpoints/slot_head/slot_best.pt \
  --host 127.0.0.1 --port 6053 --horizon 160 --replan-every 10 > /tmp/hud_serve.log 2>&1 &
sleep 5
ss -ltn | grep -q ':6053' && echo "SERVER_UP"
test -f data/hud/live.json && echo "LIVE_EXISTS_UNEXPECTED" || echo "NO_LIVE_OK"
pkill -9 -f serve_planner_command
```
Expected: `SERVER_UP` puis `NO_LIVE_OK` (sans `SYLVAN_HUD=1`, aucun `live.json` écrit).

- [ ] **Step 8: Commit**

```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
git add python/sylvan/hud/__init__.py python/sylvan/hud/live_writer.py python/sylvan/hud/test_live_writer.py data/hud/.gitignore python/scripts/serve_planner_command.py
git commit -m "Archi-HUD: écriture atomique de live.json (gate SYLVAN_HUD, inerte par défaut)"
```

---

### Task 5: Mode live (consommateur : polling + pulse)

**Files:**
- Modify: `tools/archi_hud/app.js` (remplacer le stub `startLivePolling`)

**Interfaces:**
- Consumes: `data/hud/live.json` servi à l'URL relative `../../data/hud/live.json` depuis `tools/archi_hud/` (le serveur est lancé à la racine en Task 6 ; voir note). Pour rester robuste au répertoire servi, le polling tente d'abord `live.json` à la racine du serveur puis retombe sur un chemin configurable via `window.HUD_LIVE_URL`.
- Produces: les nœuds avec `live_field` pulsent et affichent leur valeur quand `ts` change ; retour automatique en mode conceptuel si `ts` figé ou fetch en échec.

- [ ] **Step 1: Remplacer le stub `startLivePolling` dans `tools/archi_hud/app.js`**

Remplacer la fonction `function startLivePolling() { /* complété en Task 5 */ }` par :
```javascript
let LAST_TS = null;
let STALE_POLLS = 0;
const STALE_LIMIT = 6; // 6 × 500ms = 3s sans changement → mode conceptuel

function liveUrl() {
  return window.HUD_LIVE_URL || "/data/hud/live.json";
}

function fmt(field, value) {
  if (field === "command" && Array.isArray(value)) {
    return `vx ${value[0].toFixed(2)}  ω ${value[1].toFixed(2)}`;
  }
  if (field === "bearing") return `${value.toFixed(0)}°`;
  if (typeof value === "number") return value.toFixed(2);
  return String(value);
}

function setMode(live) {
  const el = document.getElementById("mode");
  el.textContent = live ? "● live" : "mode conceptuel";
  el.className = "mode " + (live ? "live" : "conceptuel");
}

function applyLive(fields) {
  document.querySelectorAll(".node").forEach((node) => {
    const field = node.dataset.liveField;
    const valEl = node.querySelector(".live-val");
    if (field && field in fields) {
      valEl.textContent = fmt(field, fields[field]);
      node.classList.remove("pulse");
      void node.offsetWidth; // relance l'animation
      node.classList.add("pulse");
    } else if (valEl) {
      valEl.textContent = "";
    }
  });
}

function clearLive() {
  document.querySelectorAll(".node .live-val").forEach((v) => (v.textContent = ""));
}

async function pollOnce() {
  try {
    const res = await fetch(liveUrl(), { cache: "no-store" });
    if (!res.ok) throw new Error("no live");
    const data = await res.json();
    if (data.ts !== LAST_TS) {
      LAST_TS = data.ts;
      STALE_POLLS = 0;
      setMode(true);
      applyLive(data.fields || {});
    } else {
      STALE_POLLS += 1;
      if (STALE_POLLS >= STALE_LIMIT) {
        setMode(false);
        clearLive();
      }
    }
  } catch (e) {
    STALE_POLLS += 1;
    if (STALE_POLLS >= STALE_LIMIT) {
      setMode(false);
      clearLive();
    }
  }
}

function startLivePolling() {
  setInterval(pollOnce, 500);
}
```

- [ ] **Step 2: Test live de bout en bout (producteur → fichier → page)**

Run (fabrique un `live.json` factice évolutif et vérifie que la page le lit) :
```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
mkdir -p data/hud
PYTHONPATH=python env_pytorch_3.12/bin/python -c "
from sylvan.hud.live_writer import write_live
write_live('data/hud/live.json', ts=1, episode=0, step=10,
           fields={'bearing': 32.0, 'min_dist': 1.8, 'command': [0.7, -0.3], 'energy': 58.0})
print('wrote live.json')
"
env_pytorch_3.12/bin/python -m http.server 8767 --bind 127.0.0.1 &
SRV=$!; sleep 1
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8767/data/hud/live.json
curl -s http://127.0.0.1:8767/data/hud/live.json | grep -q '"min_dist": 1.8' && echo "LIVE_SERVED_OK"
kill $SRV
```
Expected: `200` puis `LIVE_SERVED_OK`.

Vérification navigateur (manuelle) : depuis la racine, `env_pytorch_3.12/bin/python -m http.server 8767 --bind 127.0.0.1`, ouvrir `http://127.0.0.1:8767/tools/archi_hud/index.html`, puis ré-écrire `live.json` avec un `ts` incrémenté → le bandeau passe `● live`, les nœuds Perception (bearing), Transport (min_dist), Planner (command), Drives (energy) pulsent avec leur valeur ; arrêter d'écrire → retour `mode conceptuel` après ~3s.

- [ ] **Step 3: Commit**

```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
git add tools/archi_hud/app.js
git commit -m "Archi-HUD: mode live (polling live.json + pulse des modules)"
```

---

### Task 6: Brancher le HUD sur un vrai run + servir depuis la racine

**Files:**
- Modify: `voir_archi.sh` (servir depuis la racine pour exposer `tools/archi_hud/` ET `data/hud/`)
- Create: `run_forage_hud.sh`

**Interfaces:**
- Consumes: `run_forage_purslot.sh` (run vivant), `SYLVAN_HUD` (Task 4), le serveur statique.
- Produces: un workflow « ouvre la carte + lance un run et regarde-la s'animer ».

- [ ] **Step 1: Servir depuis la racine dans `voir_archi.sh`**

La page doit pouvoir fetch `/data/hud/live.json` ET `/tools/archi_hud/architecture.json`. Servir la RACINE, ouvrir le sous-chemin. Remplacer la fin de `voir_archi.sh` (après la validation) :
```bash
URL="http://127.0.0.1:${PORT}/tools/archi_hud/index.html"
echo "Archi-HUD → $URL  (Ctrl-C pour arrêter)"
( sleep 1; (xdg-open "$URL" >/dev/null 2>&1 || echo "Ouvre $URL dans ton navigateur") ) &
exec "$ROOT/env_pytorch_3.12/bin/python" -m http.server "$PORT" --bind 127.0.0.1
```
(On retire le `cd tools/archi_hud` : on sert depuis `$ROOT`. `app.js` fetch `architecture.json` en relatif → résolu sous `/tools/archi_hud/`, OK ; et `/data/hud/live.json` en absolu, OK.)

- [ ] **Step 2: Créer `run_forage_hud.sh`**

```bash
#!/bin/zsh
# Lance un run foraging vivant AVEC l'écriture live.json (pour regarder l'Archi-HUD s'animer).
# Ouvre d'abord la carte dans un autre terminal : bash voir_archi.sh
# Usage: bash run_forage_hud.sh [eat_radius=1.0] [horizon=160] [episodes=6]
set +e
ROOT=/home/edgarbrunet/Documents/PERSO/SylvanV1; cd "$ROOT"
mkdir -p data/hud
export SYLVAN_HUD=1
exec zsh run_forage_purslot.sh "${1:-1.0}" "${2:-160}" "${3:-6}"
```

- [ ] **Step 3: Vérifier le bout-en-bout réel (run écrit live.json qui évolue)**

Run:
```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
rm -f data/hud/live.json
bash run_forage_hud.sh 1.0 160 1 > /tmp/forage_hud.log 2>&1 &
sleep 30
test -f data/hud/live.json && echo "LIVE_WRITTEN"
T1=$(env_pytorch_3.12/bin/python -c "import json;print(json.load(open('data/hud/live.json'))['ts'])")
sleep 6
T2=$(env_pytorch_3.12/bin/python -c "import json;print(json.load(open('data/hud/live.json'))['ts'])")
echo "ts: $T1 -> $T2"
test "$T2" -gt "$T1" && echo "TS_ADVANCING"
env_pytorch_3.12/bin/python -c "import json;d=json.load(open('data/hud/live.json'));print('fields:', sorted(d['fields']))"
pkill -9 -f serve_planner_command; pkill -9 -f 'godot --path godot'
```
Expected: `LIVE_WRITTEN`, `ts` qui augmente (`TS_ADVANCING`), et `fields` contenant au moins `command`, `energy`, `min_dist` (et `bearing` dès qu'une cible est perçue).

- [ ] **Step 4: Commit**

```bash
cd /home/edgarbrunet/Documents/PERSO/SylvanV1
git add voir_archi.sh run_forage_hud.sh
git commit -m "Archi-HUD: branchement sur un run réel (run_forage_hud.sh) + service depuis la racine"
```

---

## Notes d'intégration finale

- **Maintenance** : quand un module change d'état (ex. on internalise le transport du slot → `transport_slot` passe `echafaudage`→`pur`, ou `memoire_spatiale` passe `manquant`→`pur`), éditer UNIQUEMENT `tools/archi_hud/architecture.json` (+ `focus`). Le validateur garde la carte honnête ; le diff git raconte l'évolution de l'archi.
- **Pas de couplage au pipeline** : sans `SYLVAN_HUD=1`, `serve_planner_command` est strictement identique à avant (gate inerte). Le HUD n'ajoute aucun risque au forager vivant.
